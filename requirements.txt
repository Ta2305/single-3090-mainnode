# このプロジェクトの依存関係。
#
# メインLLMはvLLMで動かす前提。GPUが1枚のみ・NVLinkなしの構成のため、
# vllm serve 実行時に --tensor-parallel-size を指定しない(=1、既定値)。

vllm==0.19.0
httpx==0.27.0
# pydanticはorchestrator/のコードでは直接使用していない(vllmが依存として
# 必要なバージョンを自動的に引き込む)。固定バージョンを指定するとvllmの要求
# (pydantic>=2.12.0)と衝突しうるため、ここでは指定しない。
pyyaml==6.0.1
tenacity==8.2.3
python-dotenv==1.0.0

# 注: SSHトンネルはPythonライブラリ(sshtunnel/paramiko)を使わず、
# OSにインストール済みの`ssh`バイナリをサブプロセスとして`-J`(ProxyJump)付きで
# 呼び出す方式を使う。`ssh`コマンドがPATHに存在することを確認しておくこと。
